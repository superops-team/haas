# Manager HaaS Backend 功能验证 Case

[English](CASES.md) | **简体中文**

Status: Draft
Change ID: manager-haas-sidecar-spec

这些 Case 是默认 Lite HaaS backend、delegated 配置更新、恢复与 AIO 兼容路径的可执行验收合同。默认离线执行；真实 Docker/Codex Case 需要显式环境开关，不可用时记录 `not_run`，不得用 mock 冒充。证据写入最终交付/PR 描述，不提交 verification 目录。

| ID | 优先级 | 需求 | 前置与命令 | 预期/通过标准 |
|----|--------|------|------------|---------------|
| FV-01 | P0 | Local bootstrap ownership、secretless、control-ready 先于 execution-ready | 临时 Manager state dir。`cd manager && uv run pytest tests/test_haas_supervisor.py tests/test_haas_delegation.py -k 'supervisor or bootstrap or startup' -q` | Lock 先于 token/ready-file；token 0600 且稳定；ready JSON 匹配 launchId/live pid；第二实例被 lock 排除；bootstrap env 不含 credential 值并使用配置的本地 profile。 |
| FV-02 | P0 | Profile/provider snapshot 唯一且规范 | `uv run --extra dev pytest tests/test_profiles.py tests/test_model_proxy.py -q` | 校验 providerId/name/wireApi/apiType；version 只增；draft 编辑使 validation 失效；session 冻结完整 route/content；delegated provider 不偏离 applied profile。 |
| FV-03 | P0 | Busy 配置更新在同一 session 有屏障地应用 | 执行 `uv run --extra dev pytest tests/test_delegated_policy_update.py -k running -q` 与 `cd manager && uv run pytest tests/test_haas_delegation.py -k pending_policy -q`。 | `/policy` 返回 202，desiredRevision 递增且公开 applied 配置保持不变；无需新 turn 即 reconciliation；排队发送仅在 appliedRevision 追平后执行一次。 |
| FV-04 | P0 | 更新失败/重启可恢复 | 注入材料化失败，在 desired commit/runtime switch 间重启。`uv run --extra dev pytest tests/test_delegated_policy_update.py -k 'failure or restart' -q` | appliedRevision 不变；有 typed failed result/event；新 turn 等待；重启从 store 恢复；修正后的更高 revision 成功；不新建 chat/session id。 |
| FV-05 | P0 | Delegated 禁止 profile-rebind | `uv run --extra dev pytest tests/test_delegated_policy_update.py -k rebind -q` | `/sessions/{id}/profile-rebind` 返回 `haas_profile_rebind_unsupported`；Manager 走 `/delegated-sessions/{id}/policy`；不 fallback/新建 session。 |
| FV-06 | P0 | 双流投影无丢失、幂等 | 以两种顺序注入 ADK/native 投影并断线 replay。`cd manager && uv run pytest tests/test_haas_stream_bridge.py -q` | 独立 scoped cursor；native terminal 不截断 ADK 文本；仅一条 assistant message/turn_end；无重复 delta；idle 配置事件走 session lifecycle channel。 |
| FV-07 | P0 | 确认 24h 过期后使用时只建一个关联新 attempt | 执行 `cd manager && uv run pytest tests/test_haas_idempotency.py -q` 与 `uv run --extra dev pytest tests/test_api.py tests/test_api_coverage.py tests/test_stores_memory.py -k expir -q`。 | 服务端返回 expiry header/readback，旧 key 返回 `haas_idempotency_expired`；无 timer 重跑；下次使用先持久化新 attempt/predecessor，只提交一次；restart/retry 复用；404/network/cursor error 不触发新工作。 |
| FV-08 | P0 | TTL/配置重建保留逻辑 session/native state | Persistent SQLite + per-session volume。`uv run --extra dev pytest tests/test_delegated_runtime.py tests/test_stores_sqlite.py tests/test_codex_recovery.py -q` | 重建 generation 递增；确定性 session volume 与 SQLite logical binding 保留；签名 receipt replay 不重复执行；Codex 能续接合法 native thread，缺失 native state 时报告 non-resumable；workspace lock 保持 single-writer。 |
| FV-09 | P0 | Mac Docker CLI 运行 Lite arm64 | Apple Silicon、本地 Docker context。`HAAS_DOCKER_BUILD=1 HAAS_PLATFORM=linux/arm64 make docker-check-lite` | digest-pinned base 可构建 arm64；hardened container health/control/execution readiness 通过；project mount 可写；generation token 为 0600 且不在 inspect env；private fake worker 首次执行与 receipt replay 通过。真实 Codex/provider 执行属于独立 FV-14，不得从 image smoke 推断。 |
| FV-10 | P0 | Lite amd64 与 AIO amd64 回归 | amd64 Docker node 或显式模拟。`HAAS_DOCKER_BUILD=1 HAAS_PLATFORM=linux/amd64 make docker-check-lite`；`HAAS_DOCKER_BUILD=1 make docker-check-aio` | Lite amd64 通过；AIO 保持 `/opt/gem/run.sh`、8080/nginx 与 sidecar 8092，禁用的可选服务保持 stopped；HaaS 公共 health 路由有效。 |
| FV-11 | P0 | Worker 无法绕过 secret/network | 静态 hardening 合同 + 真实 isolated Docker worker。`uv run --extra dev pytest tests/test_docker_build_contract.py tests/test_delegated_runtime.py -k 'isolation or token or private_worker or broker' -q`；真实容器断言同时由 FV-09 执行。 | Worker 使用 network none，不挂 Docker socket 或 broker credential；token 按 generation 隔离且为 0600；冲突、篡改、旧 generation、broker 不可用均 fail closed；credential 不进 argv/env 且无 provider 直连 fallback。 |
| FV-12 | P0 | Remote workspace 与未实现 interaction 均不可选择 | Remote endpoint/routing 与 capability matrix。`cd manager && uv run pytest tests/test_haas_endpoint.py tests/test_haas_delegation.py -k 'remote or unsupported_attachment or fails_closed' -q` | Local-project remote 执行在 delegated create 前阻断；未支持 attachment/workspace mode 明确失败；无本地 fallback。Approval/input capability 未满足 FV-16 时，HaaS-backed chat 不能选择“Ask for approval”。 |
| FV-13 | P0 | Schema、migration、release gate 对齐 | `make adk-compat`；前向迁移 fixture；OpenAPI unique-key/ref/error parity | 旧记录确定性迁移或 fail closed；native event 含 pending/applied/failed；ADK shape 兼容；门禁通过前不 advertise 2026-09-10。 |
| FV-14 | P0 release | 通过 private broker 的真实 Codex delegated execution | Digest-pinned Lite image、真实 Codex app-server、scoped model/MCP broker、测试 credential handle 和显式 `HAAS_E2E_CODEX=1` release 环境；broker transport 完成后执行仓库 E2E target。 | first turn、follow-up、cancel、reconnect/readback、usage 均经 private broker 完成；provider credential 不进入 worker。未实现或未执行前，S7 release 保持 blocked，且不 advertise 新协议版本。 |
| FV-15 | P0 release | 真实 local 执行过程可见且可 replay | 实现 `tests/test_local_process_visibility_e2e.py`；针对打包 App、隔离 state/HOME、真实 Codex 0.152.1 与已配置 Responses provider，执行 `HAAS_E2E_CODEX=1 uv run --extra dev pytest tests/test_local_process_visibility_e2e.py -q -s`。场景持续 3–5 分钟、至少五次 tool call，并在 turn 中重连 GUI。 | 同一 invocation 展示脱敏 reasoning/progress、每个 tool 的一次 start 和一次 terminal、有界 output preview、partial assistant text 与唯一 invocation terminal；reload 后顺序一致；不暴露 raw reasoning、完整参数/结果、host path 或 credential。 |
| FV-16 | P0 release | 交互审批与结构化输入续接同一 turn | 实现 `tests/test_local_interaction_e2e.py`；在 capability 为 `human_bridge` 时执行 `HAAS_E2E_CODEX=1 uv run --extra dev pytest tests/test_local_interaction_e2e.py -q -s`，覆盖 command approval、file-change approval、blocking `request_user_input`、等待期间断线重连和 approve/deny/cancel。 | 只有 capability 可用时 GUI mode 才可选；每个请求只有一张持久卡片和一次 resolution；不猜测答案、不自动批准；同一 invocation/native turn 继续；重复/过期 decision 返回 conflict。 |
| FV-17 | P0 release | 长任务 model capability 存活且失败可见 | 实现 `tests/test_model_proxy_lifecycle.py`；使用受控时间执行 `uv run --extra dev pytest tests/test_model_proxy_lifecycle.py -q`，并在 FV-15 纳入真实 provider 场景。跨过 90 秒且至少一轮 tool round-trip，注入 invalid/expired token。 | 正常长任务不丢 token；exact-scope refresh 最多一次且先 swap 后 revoke；耗尽后只产生一个带稳定 token code 的 `failed` terminal，保留 partial progress 与 replay-safe recovery；不得显示泛化成功或无原因 incomplete。 |
| FV-18 | P0 release | Invocation terminal 与 task completion 不混淆 | 实现 `manager/tests/test_haas_task_completion.py`；执行 `cd manager && uv run pytest tests/test_haas_task_completion.py -q`，覆盖显式 plan、一个必需 tool failure、verification、有界 continuation 和 budget 耗尽。 | `turn_done` 只清 busy；pending plan/verification 或非成功 invocation 使 task 保持 `verifying|incomplete|failed`；仅 completed invocation 且 blocking work 清零后完成 task；automation history 使用 task status，不使用 socket closure。 |
| FV-19 | P0 | Canonical event 完整性与有界容量 | 扩展 `tests/test_codex_normalizer.py`、`tests/test_events.py`、`tests/test_sessions.py` 和 `manager/tests/test_haas_stream_bridge.py` 并直接运行四个文件；replay 真实 0.152.1 notification fixture 与 tool-heavy 长任务。 | 保留显式 normalized type；thought 不进入 assistant text；tool lifecycle 不退化为 unparsed；每个 invocation 只有一个 terminal；配置的 coalescing 在不改变因果顺序的前提下限制持久化/投递 delta 数量。 |
| FV-20 | P0 release | 语义 Activity 投影替代机器事件行 | 新增 projector 与 transcript component test；执行 `cd manager/surfaces/gui && npm test` 以及 `cd manager && uv run pytest tests/test_haas_stream_bridge.py -q`。覆盖 command/read/search/edit/未知工具、replay、completed/non-zero exit 和重复 lifecycle update。 | 一个 `toolCallId` 只产生一个稳定 activity 并原地更新；completed 映射 succeeded；command 非零 exit 映射 failed；未知 kind 使用本地化通用工具；不得出现 `Used exec_command`、`summary=`、raw metadata key、重复 lifecycle row 或基于字符串猜 kind。 |
| FV-21 | P0 release | 完成态收敛但不隐藏失败/恢复事实 | Component 与 Playwright 场景覆盖 running、纯成功、失败后恢复、incomplete、pending verification 和手动展开。 | 活动中工作保持可见。Completed task 默认显示 final answer 与结构化结果摘要；普通成功 activity 可展开；失败/恢复、跳过验证和未解决风险保持可见。仅有 `turn_done` 不得触发收敛或成功。 |
| FV-22 | P0 release | Inspector 与窄屏抽屉响应式、可访问且 secretless | 新增聚焦 GUI Playwright spec，并分别使用 1440、1100、1099、390 CSS px 执行，覆盖纯键盘导航、展开导航、active approval/input card 与长输出；保存 desktop/mobile screenshot 并检查 accessibility state。 | >=1100 px 且 transcript 至少保留 520 px 时打开 320–400 px 右侧 Inspector；否则打开不超过 chat 可用高度一半的底部抽屉。两者不覆盖 composer 或 blocking card。Enter/Space 将焦点移至详情标题，Escape 将焦点还给 activity row，pointer 选择不抢焦点，Tab 不被捕获。状态不只依赖颜色；preview 最多五行并显示省略数；transcript/artifact action 不暴露授权脱敏存储中不存在的数据。 |
| FV-23 | P0 | 统一运行时权限审批策略端到端生效 | 执行 policy/session/API/adapter/Manager/GUI 专项测试，再用 local-managed HaaS 运行一个 workspace 内写文件并访问受控公网 HTTP endpoint 的真实任务。Turn 运行中以 `expectedRevision` 保存更严格策略；同时覆盖 stale revision、apply failure、一次精确动作审批、private/metadata URL 拒绝，以及缺少新字段的持久旧配置。 | 新 session 显式物化 `workspace-write + 公网 allow + on-request`；保留旧配置显式值，缺失字段只迁移一次。`always/on-request/never` 使用明确授权矩阵而非 rank 排序。Active invocation 保持冻结 applied revision；验证完成前 desired/applied snapshot 分离，stale revision 返回 `haas_policy_revision_conflict`，排队工作不使用 failed/stale policy。Approval 只恢复一个 native action，不改变 session policy。公网 egress 成功，private/link-local/metadata/control-plane route 与未授权 mount 仍拒绝。 |
| FV-24 | P0 | ADK 提前结束时 failed invocation 不得在前端持续显示执行中 | 在同一 HaaS session 预置超过一页的历史 invocation 事件，再启动一个 ADK stream 在 terminal 前关闭、而 canonical pages 稍后出现 tool completion 与 `haas.turn.failed` 的 invocation。执行 `cd manager && uv run pytest tests/test_haas_delegation.py -k terminal_reconciliation -q` 与聚焦 stream-bridge 测试。 | 即使当前页没有 active invocation 事件，session 分页仍持续推进。Manager 消费当前 invocation 的全部事实、以 invocation GET 确认 canonical terminal，输出安全失败与唯一 `turn_end`，将 attempt 标成 terminal、binding 从 `running` 改为 `idle`，且绝不无限等待缺失的 ADK terminal projection。 |
| FV-25 | P0 | 新提交的 prompt 与执行进展自动进入可视区 | 在 GUI Playwright fixture 中构造可滚动 transcript，将视口固定在较早历史，再提交一个会连续产生 reasoning/progress 高度变化的前台 prompt，并执行 `npm run e2e -- transcript-scroll.spec.ts`。 | 用户消息完成 DOM 提交后重新接管跟随；首个 waiting/progress 与后续实时增长始终处于最新视口，且不显示“跳到最新”。提交后用户再次主动向上滚动仍可固定阅读并显示“跳到最新”；后台/replay 更新不得抢走固定视口。 |
| FV-26 | P0 | 重试安全重连已 accepted 的未终态 attempt，并覆盖 sidecar restart | 持久化一个 local HaaS binding：current attempt 已有 accepted `invocationId` 和 native cursor，且原请求 profile 与当前 profile 不同；同时把对应 HaaS invocation/turn 持久化为 `running`，在没有 live adapter handle 的情况下重启 HaaS。执行 `uv run --extra dev pytest tests/test_s3_cancel_replay.py -k restart_reconciles_orphaned -q` 和 `cd manager && uv run pytest tests/test_haas_delegation.py -k retry_reconnects_accepted -q`。 | HaaS 使用新的 fenced lease，追加且只追加一个 retryable `haas.turn.incomplete`（`sidecar_restart_execution_lost`）；invocation/turn 变为 `incomplete`，session control 变为 `idle`，accepted idempotency reservation 使用 canonical history 完成。Manager Retry 从已存 cursor 对同一 invocation做 readback；不调用 profile sync 或 `/run_sse`，不新建 key，不重复 tool activity。仍有 live owner lease 时不得做 orphan 对账，网络/404/cursor 失败也不创建新工作。 |
| FV-27 | P0 | 本地 profile 同步可恢复部分持久化的 rebind | Manager binding 保持 profile v1，但 HaaS session 权威 effective profile 已为 v2；随后轮换本地 credential channel，并请求相同 provider/model/base URL。执行 `cd manager && uv run pytest tests/test_haas_delegation.py tests/test_haas_client.py -k 'stale_profile or equivalent_profile' -q`。 | Manager 先读取 session 权威 profile，不再从 v1 重试 CAS；可用时复用等价 active profile；确需轮换 credential handle 时最多从 v2 rebind 一次，并在 `/run_sse` 前持久化结果。真实 model/endpoint 变化仍使用 CAS，发生并发变化时冲突而非覆盖。 |
| FV-28 | P0 | 非成功终态关闭同一 turn 的全部悬空 activity | 持久化一个包含多个 running/pending/waiting tool 且随后出现 `incomplete` terminal 的 HaaS turn；通过 `cd manager && uv run pytest tests/test_haas_stream_bridge.py -q` 与 `cd manager/surfaces/gui && npm test -- --run` 覆盖 bridge replay 和 GUI transcript hydration。 | 当前 turn 每个悬空 tool 均变为 failed（cancelled/interrupted invocation 时为 cancelled）；已完成 tool 与更早 turn 不变；task 终态后没有 activity 继续显示运行中。 |
| FV-29 | P0 | 桌面热重启恢复 owned credential channel | 启动 local managed HaaS child，模拟 Manager 退出/热重启，并执行 `cd manager && uv run pytest tests/test_haas_delegation.py -k 'supervisor and (ownership or parent)' -q`；同时让配置端口存在一个健康但无关的 listener。 | Managed child 收到当前 Manager PID，并随该 Manager 退出。替代 Manager 在没有自己的 child 与 credential channel 时，绝不把前任/无关 listener 报告为 running；只做有界 drain 等待，端口释放后启动新的 owned child，否则返回 `local_sidecar_not_owned`，不附加或杀死未知进程。随后 credential grant 可通过新 channel 成功。 |

覆盖矩阵：

- Lite image/platform/runtime isolation：FV-08–FV-11、FV-14。
- Manager bootstrap/binding/interaction/completion/recovery 与桌面 activity UX：FV-01、FV-06、FV-07、FV-12、FV-15–FV-18、FV-20–FV-22、FV-24–FV-29。
- Profile/provider/MCP/skill/AGENTS.md：FV-02–FV-05、FV-08、FV-27。
- Session/event/idempotency：FV-03、FV-04、FV-06–FV-08、FV-13、FV-15–FV-22、FV-24–FV-28。
- Secretless/mount/egress/compatibility：FV-01、FV-09–FV-14。
- 统一默认值、迁移、policy mutation 与精确动作审批：FV-23。

执行顺序：FV-13/FV-19 离线 schema/event 基线 → FV-23 policy TDD 与 local-managed 验证 → FV-24 terminal reconciliation TDD → FV-25 前台跟随 TDD → FV-27 profile 恢复 TDD → FV-28 activity 对账 TDD → FV-29 supervisor ownership 恢复 TDD → FV-20 projector TDD → FV-21 transcript state → FV-22 响应式/可访问 E2E → FV-01–FV-08 集成 → FV-12 capability 回归 → FV-15–FV-18 打包交互/完成度门禁 → FV-09–FV-11 真实 Docker → FV-14 真实 delegated Codex release E2E。FV-15–FV-29 是真实会话、UI、统一 policy、终态对账、前台跟随、profile 恢复与 supervisor ownership audit 新增的 release blocker；Case 失败回到 TDD 实现并重跑影响范围。
